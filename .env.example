# Template — variáveis de ambiente do site institucional.
# O frontend não precisa de nenhuma variável no momento.
# Secrets usados apenas pelo backend PHP ficam em secrets do GitHub
# e são injetados em dist/api/config.local.php via .github/workflows/deploy.yml:
#   - ERP_API_URL
#   - ERP_API_KEY
